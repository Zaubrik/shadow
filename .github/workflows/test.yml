name: test
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-16.04
    steps:
      - uses: actions/checkout@master
      - uses: denolib/setup-deno@master
        with:
          deno-version: x
      - run: deno test -A
